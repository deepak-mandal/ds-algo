package com.stackroute;

/*
import static org.assertj.core.api.Assertions.assertThat;
import static org.junit.jupiter.api.Assertions.*;

import com.stackroute.controller.UserAuthenticateController;
import com.stackroute.model.User;
import org.junit.Test;
import org.junit.jupiter.api.BeforeEach;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.web.bind.annotation.RequestBody;

import javax.servlet.ServletException;
import java.util.HashMap;
import java.util.Map;
*/
/**
 * Unit test for simple App.
 */
//@SpringBootTest
public class UserServiceAppTest
{
    /**
     * Rigorous Test :-)
     */
    /*
    @Test
    public void shouldAnswerWithTrue()
    {
        assertTrue( true );
    }

     */
   /* private User user;


    @Autowired
    private UserAuthenticateController userAuthenticateController;
    //private Map<String, String> tokenMap;

    @BeforeEach
    public void setup() {
        user = new User("deepak1@cgi.com", "deepak1", "deepak");
    }
    String email="deepak1@cgi.com";
    String password="deepak1";
    String jwtToken="";
    Map<String, String> map = new HashMap<>();
/*

    @Test
    public void givenAUserThenCallToGenerateTokenShouldReturnNotNull() throws Exception {
        //jwtToken = UserAuthenticateController.login(User user);
        jwtToken = userAuthenticateController.getToken("email","password");
        assertNotNull(jwtToken);
    }

    @Test
    public void givenAUserThenShouldReturnNotNullValue() throws ServletException {
        map = (Map<String, String>) userAuthenticateController.login(user);
        assertNotNull(map);
    }

    @Test
    public void givenAUserThenShouldReturnExpectedClaimInMap() throws ServletException {
        map.clear();
        map = (Map<String, String>) userAuthenticateController.login(user);
        assertThat(map.get("message")).isEqualTo("user successfully logged in");
    }
*/




}
